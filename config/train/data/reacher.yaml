dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: dmc/reacher_random
  keys_to_load:
    - pixels
    - action
    - observation
  keys_to_cache:
    - action
    - observation
