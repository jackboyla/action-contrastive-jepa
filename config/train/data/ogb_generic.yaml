dataset:
  # Override this at launch, e.g. data.dataset.name=ogbench/visual_puzzle_4x4_play
  name: ???
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  keys_to_load:
    - pixels
    - action
  keys_to_cache:
    - action
