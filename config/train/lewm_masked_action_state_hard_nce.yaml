defaults:
  - lewm_masked_action_state_nce
  - _self_

output_model_name: lewm_masked_action_state_hard_nce

loss:
  masked:
    state_nce:
      mode: absolute
      hard_negatives: 64

wandb:
  config:
    name: ${output_model_name}
