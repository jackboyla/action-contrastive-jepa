defaults:
  - launcher: local
  - solver: cem
  - _self_

world:
  env_name: swm/TwoRoom-v1
  num_envs: ${eval.num_eval}
  max_episode_steps: ??? # make sure it's >= eval_budget
  history_size: 1
  frame_skip: 1

seed: 42
policy: random # ckpt name or random

dataset:
  stats: ${eval.dataset_name}
  keys_to_cache:
    - action
    - proprio

plan_config:
  horizon: 5
  receding_horizon: 5
  action_block: 5 # frameskip

# Longer TwoRoom protocol described in the paper text. The released HF checkpoint
# does not reproduce the reported TwoRoom figure under this setting.
eval:
  num_eval: 50
  env_batch_size: null
  goal_offset_steps: 100
  eval_budget: 150
  img_size: 224
  dataset_name: tworoom
  callables:
    # -- set state
    - method: _set_state
      args:
        state:
          value: proprio
    # -- set goal state
    - method: _set_goal_state
      args:
        goal_state:
          value: goal_proprio

output:
  filename: tworoom_long_results.txt
  save_video: true
  save_planning_artifacts: false
  planning_artifacts_dir: null
